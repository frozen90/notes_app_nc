type Notes @model @auth(rules:[{allow: owner}]){
  id: ID!
  title: String!
  content: String
}


type SharedNote @model{
  id: ID!
  title: String
  content: String
  expire_date:AWSDateTime
  password: String
}
